# Surch ⟷ matchID gap analysis

Rolling table of every matchID requirement filed in
`docs/wp-d-matchid/incoming/`, mapped to its Surch-side implementation
status. Updated on every commit that affects this WP.

Status legend:

- **gap** — matchID needs it, Surch does not implement it yet
- **partial** — Surch implements a subset; mismatch documented in the
  decision file
- **implemented** — Surch implements it on `main`, gated by at least
  one of: cargo test, the SciFact NDCG@10 gate, the INSEE artillery
  harness, the matchID replay fixture under `tests/matchid_compat/`
- **declined** — out of scope by mutual agreement; decision recorded
  under `decisions/`

| Gap | Title | Workload | Status | Surch artifacts | Notes |
|---|---|---|---|---|---|
| **A1** | `match` object form with `fuzziness` sub-field | name lookup | implemented | `crates/surch-api/src/search.rs::parse_match_query` | object body routes to `SearchQuery::Fuzzy`; AUTO low=3 high=6 (edits=1 for <6-char terms, edits=2 otherwise) |
| **A2** | `geo_point` + `geo_distance` | UI "near X" filter | partial (no geo_bounding_box, no geo_polygon) | `crates/surch-index/src/mapping.rs` (`FieldType::GeoPoint`); `crates/surch-api/src/search.rs` (`SearchQuery::GeoDistance`, `parse_geo_distance_query`, haversine executor); `crates/surch-api/tests/search.rs` (5 `search_router_a2_*` cases) | accepts geo_point in 3 shapes (object, `"lat,lon"` string, GeoJSON `[lon,lat]` array); units km/m/mi/yd/ft/NM case-insensitive; haversine on WGS-84 6 371 008.8 m sphere; constant `_score = 1.0` (filter, not scorer); `geo_bounding_box` and `geo_polygon` out of scope for matchID's current wire shape |
| **A3** | `bool.filter` / `bool.should` / `bool.must_not` / `minimum_should_match` / clause `boost` | every advanced search | implemented | `crates/surch-api/src/search.rs` (`SearchQuery::Bool`, `parse_bool_query`, `parse_minimum_should_match`, `parse_boost`); `crates/surch-api/tests/search.rs` (12 `bool_*` cases incl. `must_not` and the matchID nested-should/boost shape); `tests/matchid_compat/replays/deces_v1.json` | filter intersects without scoring; should supports integer + `"N%"` MSM (default 1 when only `should` is present); `must_not` excludes matching documents without contributing to `_score`; clause `boost` multiplies the `Bool` `_score`; per-leaf clause boost still left for a follow-up |
| **A4** | Scroll API | bulk-match | implemented | `crates/surch-api/src/scroll.rs` (`ScrollContext`, `ScrollTable`, base64 ids, TTL parsed from `1m`/`30s`/`2h`, lazy GC); `crates/surch-api/src/state.rs` (`AppState::scroll_table`); `crates/surch-api/src/search.rs` (parser of `?scroll=` query param, `_scroll_id` in `SearchResponse`); `crates/surch-api/src/lib.rs` (`POST /_search/scroll` route); `crates/surch-api/tests/scroll.rs` (3 lifecycle cases); B1 v2 phase 2 lifts the 3 scroll skips and asserts `_scroll_id` on each initial response | `?scroll=1m` (and `30s`, `2h`) parsed on `POST /:index/_search`, returns the response with a base64 `_scroll_id`. `POST /_search/scroll` looks up the cursor (404 on unknown id), advances by `size`, re-inserts the context if the cursor has not exhausted, and returns the next page. GC is lazy on every insert/take (no background timer), which is enough for matchID's 1m window. |
| **A5** | `function_score` wrapper + scoring functions | advanced + block match | partial (gauss is YYYYMMDD-only; `linear`/`exp` decay, `script_score`, `random_score` deferred to phase 3) | `crates/surch-api/src/search.rs` (`SearchQuery::FunctionScore`, `ScoringFunctionClause`, `ScoringFunction`, `FieldValueModifier`, `ScoreMode`, `BoostMode`, `parse_function_score_query`, `parse_scoring_function_clause`, `parse_field_value_factor_function`, `parse_gauss_decay_function`, `combine_scoring_functions`, `combine_with_boost_mode`, `evaluate_scoring_function`, `apply_field_value_modifier`, `parse_yyyymmdd_to_days`, `parse_day_duration`); `crates/surch-api/tests/search.rs` (8 `search_router_a5_*` cases incl. 5 phase-2 cases) | Phase 1 (no-op wrapper): parses `function_score { query, boost?, functions?: [], score_mode?, boost_mode?, max_boost?, min_score? }`, forwards to the inner query multiplied by `boost`. Phase 2 (this batch) honours non-empty `functions[]` per intake §2.2 wire shape: `weight` (constant factor — also as bare `{ weight: N }`), `field_value_factor` (factor + `modifier ∈ { none, log1p, sqrt, reciprocal }` + `missing`), `gauss` decay over a `YYYYMMDD` keyword date field (origin / scale-in-days / decay). Each clause supports an optional `filter` (per-function gating; non-matching docs contribute the score-mode identity — 1.0 for Multiply, 0.0 for Sum/Avg, skip for First/Min/Max) and an outer `weight` multiplier. Per-function factors are combined via `score_mode ∈ { multiply, sum, avg, first, min, max }`, then composed with the inner BM25 `_score` via `boost_mode ∈ { multiply, sum, avg, replace, min, max }`. Empty `functions: []` keeps the fast path so the SciFact NDCG@10 baseline is preserved (no scoring-function code touched). `linear`/`exp` decay, `script_score`, `random_score` and non-date `gauss` rejected with an explicit "phase 3" HTTP 400. |
| **A6** | `prefix` query + `index_prefixes` mapping option | autocomplete | implemented (text fields) | `crates/surch-api/src/search.rs` (`SearchQuery::Prefix`, `parse_prefix_query`, `prefix_field_matches`, `prefix_token_spans`, `posting_candidate_ids::Prefix` branch); `crates/surch-index/src/document_index.rs` (`prefix_postings`, `field_has_prefix_postings`, `index_prefix_terms`, 3 unit tests); `crates/surch-index/src/mapping.rs` (`FieldPrefixes`, `FieldMapping::index_prefixes`, `parse_index_prefixes`, `MappingError::IndexPrefixes*`); `crates/surch-api/src/state.rs` (`InMemoryIndex::prefix_hits`, `AppState::documents_for_prefix`); 3 `from_properties_value_*_index_prefixes_*` mapping tests + 3 `prefix_query_uses_index_prefixes_postings_*` integration tests | Phase 1 parses the field-level `index_prefixes: { min_chars, max_chars }` option (matchID `PRENOMS` shape). Phase 2 ships the runtime acceleration: at indexing, each analyzed token of a text field carrying `index_prefixes` fans out into a side-table `BTreeMap<field, BTreeMap<prefix, BTreeSet<doc_id>>>` for every length in `[min_chars..=max_chars]`. At query time, `posting_candidate_ids` short-circuits `SearchQuery::Prefix` to a direct lookup when the normalized prefix length sits inside the declared window; otherwise (or when the field lacks `index_prefixes`) execution falls back to the existing `prefix_field_matches` source-scan. The side-table is dropped on rebuild and never touched by BM25 (separate from `PostingsBuilder`). matchID's keyword-field prefix workload (`DATE_NAISSANCE` short-input) remains parked behind a future A6/A13 keyword-prefix iterator. |
| **A7** | `range` on `date{format:yyyyMMdd}` | date filters | partial | `crates/surch-index/src/mapping.rs` (`DEFAULT_DATE_FORMAT`, `FieldMapping::date_format`, `with_date_format`, `MappingError::DateFormatNot{String,Supported}`); 4 new unit tests (`from_properties_value_*_date_*`) | date type parsed as keyword-string with optional `yyyyMMdd` format; `format` captured verbatim (forward-compat with `epoch_millis`, `strict_date_optional_time`, custom patterns), defaults to `yyyyMMdd`, rejected on non-date fields. Range matching reuses the existing keyword `range`/`parse_range_query` path — INSEE `DATE_NAISSANCE` is already `YYYYMMDD` lex-sortable. Runtime date math (chrono parsing, epoch conversion, ES `now-1y/d`) out of scope. `FieldMapping` lost `Copy` to own `String` — three callers in surch-api swapped `*mapping` for `mapping.clone()`. |
| **A8** | `match_all` | default + filter context | implemented | `crates/surch-api/src/search.rs::parse_match_all_query` | accepts `{}` and `{ "boost": N }`; contributes `boost` (default 1.0) to bool-must sums; standalone form keeps `_score = null` parity |
| **A9** | `from` + `size` pagination | result paging | implemented | `crates/surch-api/src/search.rs` (`run_topk_search`, `paginate_hits`, `MAX_RESULT_WINDOW`); `crates/surch-api/tests/search.rs` (3 `search_router_a9_*` cases) | from/size already wired in the topk shortcut + full-scan paths; this batch adds the Elasticsearch `index.max_result_window = 10 000` cap (returns HTTP 400 `search_phase_execution_exception` when `from + size > 10 000`) and pins the contract via tests |
| **A10** | `sort` over keyword / normalised-date sub-fields | UI table | implemented (write-time fan-out landed; query-side consumption of the stored projection deferred to A1/A12 follow-up) | `crates/surch-api/src/search.rs` (`sort_scored_documents`, `compare_sort_clause`, `lookup_sort_value`, `normalize_sort_value`); `crates/surch-index/src/mapping.rs` (`FieldMapping.fields`, `FieldMapping::{with_subfields,subfields,has_subfields}`, `IndexMapping::resolve_field`, `IndexMapping::subfield_normalizer`, recursive `parse_field_definition`, `MappingError::{SubfieldsNotObject,EmptySubfieldName,NestedSubfieldsNotSupported}`); `crates/surch-index/src/document_index.rs` (`subfield_values` side-table, `index_subfields`, `subfield_terms`, `DocumentIndex::{subfield_value,has_subfield_values,subfield_values_map}`, 5 fan-out unit tests); `crates/surch-index/src/memory.rs` (`subfield_values_bytes` accounting + test); `crates/surch-api/tests/search.rs` (4 `search_router_a10_phase2_*` cases) + `crates/surch-index/src/mapping.rs::tests` (5 `*multi_field*` / `*subfield*` cases) | Phase 1 wired the `NOM.raw` / `DATE_NAISSANCE.norm` sub-field → parent alias (`lookup_sort_value`) so matchID's wire shape sorts deterministically. Phase 2 parsed the ES multi-field block `NOM: { …, fields: { raw: { type: keyword, normalizer: norm } } }` (intake §2.12), round-trips it through `GET /:index/_mapping` (`as_value`), and applies the sub-field's `normalizer` at sort time. Phase 3 (this batch — write-time fan-out) re-analyzes each parent value with the sub-field's own chain at index time: a `keyword` sub-field with a `normalizer` stores the WHOLE value as one lowercased/asciifolded token (ES applies a normalizer to the single keyword token, it never tokenizes — `"Étienne DUPRÉ"` → `"etienne dupre"`), a plain `keyword` sub-field stores it verbatim, a `text` sub-field is tokenized by its analyzer. The fanned-out tokens are written to the regular postings under the qualified `parent.sub` path (so `term`/`match` on the sub-field resolve through the FST) and recorded as a per-doc stored projection in a `subfield_values` side-table that sort/agg can read directly via `DocumentIndex::subfield_value`. The read-time `lookup_sort_value` alias is kept as the durable fallback; threading `DocumentIndex::subfield_value` into `sort_scored_documents` / `compute_*_aggregation` so `sort`/`agg.cardinality` on `.raw` consume the stored projection without any source-scan is the remaining query-side wiring, deferred to the A1/A12 follow-up. Nested `fields` and unknown sub-field types are rejected with explicit `MappingError`s. |
| **A11** | `min_score` top-level body filter | full-text | implemented | `crates/surch-api/src/search.rs` (`SearchRequest::min_score`, `parse_min_score`, filter in `run_search`); `crates/surch-api/tests/search.rs` (4 `search_router_a11_*` cases) | accepts non-negative finite f64; filter applied only when scoring is enabled (no-op on `match_all` / `term` / `range` standalone, per Elasticsearch); total hits reflects post-filter count; top-K shortcut falls back to full-scan when `min_score` is set |
| **A12** | Aggregations | analytics tab | partial (terms + date_histogram + cardinality + composite terms-only; composite date_histogram sources pending) | `crates/surch-api/src/search.rs` (`SearchRequest::aggs`, `AggSpec::{Terms,DateHistogram,Cardinality,Composite}`, `CompositeSource`, `CalendarInterval`, `parse_aggs`, `parse_terms_agg`, `parse_date_histogram_agg`, `parse_cardinality_agg`, `parse_composite_agg`, `compute_aggregations`, `compute_terms_aggregation`, `compute_date_histogram_aggregation`, `compute_cardinality_aggregation`, `compute_composite_aggregation`, `AggResult::{Buckets,Value,Composite}`, `AggBucket`); `crates/surch-api/tests/search.rs` (14 `search_router_a12*` cases) | A12.1 shipped the `terms` agg MVP (top-`size` buckets ordered by `doc_count` desc + key asc tiebreak, `size` defaults to 10). A12.2 adds `date_histogram` over the A7 `date{format:yyyyMMdd}` keyword-encoded storage: each matched doc is truncated to `calendar_interval` (`day`/`week`/`month`/`year`, plus `1d`/`1w`/`1M`/`1y` aliases) and counted into the bucket; buckets emitted sorted by key ascending (parity with ES); `key_as_string` is set when the request declared a `format`. A12.3 adds `cardinality` returning an exact distinct-value count (no HyperLogLog estimation — MVP); `precision_threshold` is parsed and ignored. A12.4 adds `composite` (terms sources only) with `after_key` cursor round-trip: each matched doc projects a composite key via `lookup_sort_value` for every source, buckets are emitted sorted lexicographically (source-by-source ascending — parity with ES), capped to `size` (default 10); `after` is applied as a strict `>` filter on the sorted key stream, and `after_key` is emitted only when the cap actually truncated the stream (otherwise omitted to signal end-of-stream). Composite `date_histogram` (and other non-`terms`) sources are rejected with an explicit "A12.4 phase 2" parsing_exception. All aggs honour the `.raw` / `.norm` sub-field alias via `lookup_sort_value`. Response envelope is an untagged `AggResult` enum so `terms`/`date_histogram` emit `{buckets:[...]}`, `cardinality` emits `{value:N}`, and `composite` emits `{buckets:[...], after_key?:{...}}` on the same `aggregations.<name>` key. `histogram` (numeric bucketing) and other non-matchID agg kinds still return the deterministic `parsing_exception` carrying "tracked in A12 phase 2". |
| **A13** | Mapping primitives | index time | partial | `crates/surch-analysis/src/lib.rs` (`EdgeNgramAnalyzer`, `NormAnalyzer`, `Normalizer`, `asciifold_char`/`asciifold_string`); `crates/surch-index/src/mapping.rs` (`AnalyzerName::Norm`, `AnalysisSettings`, `EdgeNgramTokenizerDefinition`, `AnalyzerDefinition`, `NormalizerDefinition`, `IndexMapping::from_index_settings_value`, field-level `normalizer`); 9 new unit tests (`crates/surch-analysis/src/lib.rs::tests` + `crates/surch-index/src/mapping.rs::tests`) | Ships the analyzer + tokenizer + normalizer trio matchID needs to replay `deces_index.yml`'s `settings.analysis` block (verbatim shape from §2.12 of the 2026-05-15 intake). ASCII folding now uses Unicode NFD decomposition plus combining-mark stripping for decomposable diacritics, with explicit overrides for non-decomposable ligatures / barred or stroked letters and punctuation cases such as `Æ/æ`, `Œ/œ`, `ß/ẞ`, `Ł/ł`, `Đ/đ`, `Ø/ø`, `Þ/þ`, quotes and dashes. This improves Unicode coverage beyond the earlier French-only table, but it is still not a byte-for-byte full port of Lucene's `ASCIIFoldingFilter`; unlisted compatibility variants may still pass through unchanged. `token_chars` (letter/digit) captured but not enforced — we fall back to standard alphanumeric tokenisation. Still gap on this row: `date{format}` (tracked separately under A7); `geo_point` now ships under A2. `index_prefixes` ships under A6 (parsing in phase 1, write-time acceleration in phase 2 for text fields). |
| **A14** | Elasticsearch response shape | client compat | partial | `crates/surch-api/src/search.rs::resolve_total_hits`, `SearchHitsTotal`; `crates/surch-api/tests/search.rs` (4 `search_router_a14_*` cases) | `hits.total.{value,relation}` matches Elasticsearch: `"eq"` when uncapped, `"gte"` when capped by `track_total_hits=N` or by the default 10 000-doc cap. `_scroll_id` (A4) and `aggregations.<name>.buckets` (A12) remain out of scope here and are tracked under their own gaps. |
| **A15** | `_msearch` inner DSL parity | block-match | partial | `crates/surch-api/src/msearch.rs` (NDJSON wrapper shipped) | inner-query failures are the same as A1..A12 |
| **B1** | matchID replay fixture | acceptance | implemented; Elasticsearch 8.6.1 oracle PASS | `tests/matchid_compat/replays/deces_v1.json`, `crates/surch-api/tests/matchid_compat.rs`, `deploy/k8s/jobs/b1-oracle-gate.yaml`, `docs/ops/bench-reports/2026-05-20-b1-oracle-ES861-K8s/` | v1: 30 representative requests (8 advanced + 5 block-match + 4 full-text + 3 sort + 2 function_score + 3 prefix + 3 scroll + 2 range). B1 phase 2 lifted the 3 scroll skips (gap-A4) by asserting `_scroll_id` is present in the initial response (the follow-up `POST /_search/scroll` lifecycle is covered separately by `crates/surch-api/tests/scroll.rs`). A3 closure lifted the final `bool.must_not` skip, so the fixture executes all 30 requests. K8s run `26192816780` replayed the fixture against Surch and Elasticsearch 8.6.1 on `main` `466693f`, with 30 requests, 0 skipped, 0 divergence, and oracle exit 0. Phase 4 widening (`deces_v2` INSEE replay and broader mapping/query coverage) is deferred to a follow-up plan. |
| **B2** | INSEE 10k slice frozen fixture | acceptance | implemented (v1 — real INSEE 10k `Deces_2024` slice + synthetic 1000-doc fixture kept for B1 replay) | `tests/matchid_compat/deces/{mapping.json,slice-1000.ndjson,slice-10000.ndjson.gz,README.md}`, `tools/{gen_deces_slice.awk,fetch-insee-slice.sh}`, `crates/surch-api/tests/matchid_insee_slice.rs` | B2 v1 ships a real INSEE Open Licence 2.0 extract (first 10 000 rows of `Deces_2024.csv` — `https://www.insee.fr/fr/information/4769950`) at `tests/matchid_compat/deces/slice-10000.ndjson.gz` (sha256 `1f71d52c554900fbfb055be75ddff4fc04bb891cbce8725295f5fd7e68eace02`, 357 kB, byte-stable via `gzip -9 -n`) plus a sanity-load test (`matchid_insee_slice_b2_v1_loads_and_serves_match_query`) that bulk-loads it against the matchID `mapping.json` and asserts a `match NOM=RUMIANO` query returns the row-1 record `ins_0000001`. The synthetic `slice-1000.ndjson` is kept (and remains source of truth for the deterministic `deces_v1.json` replay — its per-request `top_id` expectations are pinned to the AWK byte stream and would need a full re-capture to track the INSEE corpus; deces_v2 replay against the INSEE slice is gated on the B1 phase 3 Elasticsearch 8.6.1 oracle cross-check). Mapping still restricted to `text`/`keyword`/`integer`; will be widened to multi-fields + `date{format}` + `geo_point` + `edge_ngram`/`normalizer` when gaps A1/A2/A7/A13 land (A6 `index_prefixes` already shipped). |
| _confirmed_ | `match`, `multi_match`, `bool.must`, `term`, `fuzzy` standalone | interactive lookup | implemented | `crates/surch-api/src/search.rs` parsers + executors | matched against `tests/opensearch_compat/` replay |
| _confirmed_ | highlight | response shaping | implemented | `crates/surch-api/src/search.rs` highlight path | |
| _confirmed_ | `_search?track_total_hits=`, `_mget`, `_count` | id lookup + total | implemented | `crates/surch-api/src/{search,mget,count}.rs` | |

## How to read this table

- `Gap` = stable id used in commit subjects and PR titles
- `Workload` = which matchID code path triggers it (1-2 words)
- `Surch artifacts` = source files, test fixtures and bench reports
  that close the gap; bullet-list when several
- `Notes` = link to the decision file under
  `docs/wp-d-matchid/decisions/` and any scope adjustment we agreed on
