# Verdict 28M sous sonde honnête + compression `_source` — 2026-07-11/12

Suite de `brainstorm-4-fronts-2026-07-09.md` (front 1 : sonde aléatoire) et
`contre-expertise-2b-source-2026-07-10.md` (compression PAR-DOC zstd). Tous les runs : harnais
`deploy/bench-local/fair-ab.sh`, corpus réel deces 28 917 511 docs (14,7 Go NDJSON), budgets
`SURCH_FLUSH_BUDGET_BYTES=268435456 SURCH_MERGE_FANIN=8 SURCH_DENSIFY_BUDGET_DOCS=1000000
SURCH_MERGE_MAX_DOCS=7000000 POSTINGS_DISK=1`, gouverneur powersave (biais assumé, identique
partout). « RSS conteneur » = mémoire physique du cgroup (heap + page cache, `memory.current`).
Latence TOUJOURS qualifiée par le profil de sonde : fixe (`nom:MARTIN`, best-case cache) /
aléatoire (Zipf naturel tiré du corpus, `size:10`, LCG graine 42) / froide (cache évincé,
`docker update squeeze`).

## 0. Post-mortem machine (nuit du 10-11/07) et plafonnement du harnais

OOM **global** de l'hôte (57 Go, ~10 sessions Claude, ~20 conteneurs tiers, swap 8 Go plein) : le
noyau a tué des processus Claude de 9,2 puis 16,4 Go anon (22h16, 03h42). Le surch du bench, lui,
est mort DANS son cgroup 4g (mécanisme voulu). Failles harnais corrigées (commit `d70f624`) :
conteneurs auxiliaires (feeder split 14,7 Go, sondes) **sans cap mémoire**, partie hôte non bornée,
`OUT_DIR` par défaut sur tmpfs. Garde-fous ajoutés : scope systemd `MemoryMax=3G` (re-exec),
préflight `MemAvailable ≥ cap conteneur + scope + 2 Gio`, verrou flock mono-run, `AUX_MEM=512m`
sur les 13 conteneurs auxiliaires, trap teardown, fix `mem_to_mib` mawk. Le préflight a depuis
refusé 3 lancements sur machine chargée — comportement voulu.

## 1. Le plancher « 4g » est INVALIDÉ (axe RSS conteneur)

Le PASS 28M@4g du 06/07 (sha `b795b10`, RSS 3,006 Gio) n'est **pas reproductible** : 2 OOM sur
3 runs à invocation strictement identique (image, budgets, corpus, `ENGINES=surch`). Signature
identique au kilo-octet près : bulk 28,9M intégralement indexé, mort au refresh final avec
anon = 4 150 924 ko puis 4 150 756 ko → **le transient de matérialisation finale vaut ~4,15 Go
anon quasi déterministe** ; le PASS était passé sous le plafond d'un cheveu. Plancher réel surch
28M : **]4g flaky, 6g sûr]** (deux PASS @6g ce jour, RSS ~3,0 Gio) ; ES tient @4g → l'axe
plancher de survie reste PERDU. Levier identifié : packing side-table `_source` 8 o/doc
(−430 Mio anon, cf. §4).

## 2. Front 1 livré : latences honnêtes @28M — la claim fixe ne survit pas

| Sonde (28M) | surch @6g (run B, sans zstd) | ES @4g (10/07) | ES @6g |
|---|---|---|---|
| Fixe p50/p95/p99 (ms) | 0,35 / 0,52 / 0,68 | 1,38 / 2,50 / 3,18 | à faire (run D, préflight refusé ×2 sur machine chargée) |
| **Aléatoire** | **7,37 / 85,17 / 122,51** | **4,83 / 15,86 / 51,57** | idem |
| Froide | 5,53 / 76,70 / 105,12 | 4,89 / 16,37 / 49,90 | idem |

La domination surch 2,5-3,4× (sonde fixe) s'inverse en **~5× de retard au p95 aléatoire, avec
2 Gio de budget en PLUS** : chaque requête fetche ~10 `_source` dispersés dans 13,8 Go non
compressés avec ~680 Mio de cache fichier résiduel → p95 = seeks disque nus. ES (stored fields
compressés, ~10,3 Go total) encaisse. Nota : pour ES, aléatoire ≈ froide → à cette échelle le
trafic aléatoire est déjà quasi-cold pour les deux moteurs. Run B complet : 21 366 doc/s
(indexation), RSS 3,016 Gio, disque 18 568 Mio **enfin ventilé** : `_source` 13 824 Mio (74 %),
postings 4 598, subfields 1 047, fst/other 0 (12 segments).

## 3. Tranche 2b : compression PAR-DOC zstd — gates et mesures

Oracle de parité (`oracle-local.sh`, image `sha-d70f624c…`) : **0 divergence** flag on ET off ✓.

**Gate 1,36M@1536m** (vs réf. `b795b100`, seule diff code = `d3ad8e6`) : disque 1040 → 784 Mio
(−25 %), `_source` 640 → 384 Mio (×1,67) ; latence aléatoire AMÉLIORÉE 0,97/3,16/4,85 →
0,66/2,37/3,83 ms ; anon +0,3 % ; indexation 22 242 → 20 754 doc/s (−6,7 %, seuil ≤5 % dépassé).

**Run C 28M@6g avec `SURCH_SOURCE_COMPRESS=1`** (vs run B, même budget) :
- **Disque 18 568 → 12 296 Mio (−34 %)** ; `_source` 13 824 → 7 248 Mio (**ratio ×1,91**) —
  gate « ≤13-14 Gio » **PASS** (12,0 Gio), reste 1,19× le disque ES@4g (10 342 Mio).
- RSS conteneur 2,99 Gio, anon −0,5 % ✓ (side-table inchangée, attendu).
- ⚠️ **Indexation 16 742 doc/s (−21,6 %)** — bien au-delà du coût zstd inline prévu (~5 %).
- ⚠️ **Latence aléatoire 13,92/127,01/202,52 ms (pire de ~50-65 %)** — l'INVERSE du 1,36M.

**Anomalies non tranchées** — confonduants sérieux : les runs B et C n'ont pas tourné dans le
même état d'hôte (C : docker fraîchement redémarré après mort du démon à 17h05, swap plein,
cache fichier au moment de la sonde 171 Mio vs 678 au run B) ; le run C initial a d'ailleurs été
tué à mi-course par ce redémarrage du démon (collatéral, pas un bug moteur). Analyse code Codex
(GPT-5.5 xhigh) en cours sur : placement de la compression sous lock du chemin d'append, création
de contexte zstd par doc, niveau effectif. **À trancher avant toute conclusion : re-run B/C
dos-à-dos dans un état machine comparable + rapport Codex.** Aucun gate latence/indexation n'est
déclaré FAIL sur cette seule paire de runs.

## 3bis. TRIPTYQUE VM (2026-07-12) — anomalies TRANCHÉES, face-à-face complet @6g

Machine dédiée calme (SCW PRO2-M 16 vCPU/64 Go, stockage bloc SBS, `vm-triptych.sh`,
autorisation user) — les chiffres absolus ne sont PAS comparables au laptop (EPYC + SBS vs
Ryzen + NVMe : latence aléatoire ~4× plus haute pour tous), seules les comparaisons INTERNES
comptent. Corpus/disque identiques au Mio près entre laptop et VM (déterminisme confirmé).

| Axe @6g, 28,9M docs | surch réf (B') | surch zstd (C') | ES 8.6.1 (D') |
|---|---|---|---|
| Indexation (doc/s) | 12 197 | 11 776 (**−3,5 %**) | 12 148 |
| RSS conteneur | 3,135 Gio | **3,04 Gio** | 4,332 Gio |
| RAM anonyme (warm) | 3 228 Mo | 3 217 Mo | 3 835 Mo |
| Disque (Mio) | 18 568 | 12 296 (`_source` ×1,91) | **9 115** |
| Fixe p50/p95/p99 (ms) | **1,44/1,69/1,89** | 1,45/1,67/1,90 | 3,11/4,09/5,42 |
| Aléatoire | 29,6/225,5/297,6 | 28,2/243,0/322,4 (p95 **+7,8 %**) | **11,5/58,8/108,1** |
| Froide | 29,2/228,9/329,4 | 28,7/253,6/337,9 (p95 **+10,8 %**) | **9,5/52,4/102,9** |

**Anomalies du §3 TRANCHÉES = contamination d'hôte** : sur machine calme, le coût d'indexation
zstd est −3,5 % (gate ≤5 % PASS ; le −21,6 % laptop était un artefact) et la latence aléatoire
p95 coûte +7,8 % (gate <10 % PASS ; le +50 % laptop était un artefact). Seule la froide p95
(+10,8 %) dépasse marginalement. **Tous les gates 2b passent** (oracle 0 div, disque 12,0 Gio
≤13-14, anon inchangé).

**Face-à-face à budget égal** : parité indexation ; surch gagne RAM (RSS −30 %, anon −16 %) et
latence fixe (2,2×) ; ES gagne disque (1,35× après zstd, contre 2× avant) et **latence
aléatoire/froide ~4× au p95 — structurel, confirmé hors artefacts**. Piste chiffrée pour le
prochain cycle : le p50 aléatoire surch ≈ 10 preads `_source` SÉQUENTIELS × latence disque
(28 ms ≈ 10 × 2,8 ms SBS) vs ES 11,5 ms → **paralléliser les fetchs `_source` d'un top-k**
(io parallèle par hit) attaquerait directement le facteur 2-3 du p50, indépendamment du cache.

## 4. Prochaines étapes (ordre)

1. Run D (ES @6g) dès marge machine — complète le face-à-face à budget constant.
2. Rapport Codex : trancher les anomalies indexation/latence du run C ; si cause code → fix puis
   re-gate ; si variance hôte → re-run B/C dos-à-dos.
3. Packing side-table 40 bits offset + 24 bits length (8 o/doc, −430 Mio anon) — plan Codex en
   cours ; vise à rendre le 4g atteignable (transient 4,15 → ~3,7 Go).
4. Si `_source` compressé doit descendre sous ~6 Gio : dictionnaire zstd entraîné (escalade
   contrat 2b).
5. Warm fadvise hiérarchisé (front 2, XS) : le cache résiduel post-indexation est quasi nul
   (171-680 Mio sur 2,7 Gio de marge) — un warm dirigé du `_source` chaud changerait le p95
   aléatoire des deux variantes.

Artefacts : scorecards JSON sous le scratchpad de session (`fair-ab-28M-6g{,-compress}/`,
`fair-ab-verdict-28M-4g/`, `fair-ab-gate-1536m{,-compress}/`), logs kernel cités dans le
journal de session.
