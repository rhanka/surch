# Lucene Parity Tests

This directory holds golden fixtures and integration tests derived from the pinned Lucene reference in `docs/portage/REFERENCES.md`.

Every Lucene parity test must name the upstream commit, file, class, method, fixture generator, and tolerated differences.
