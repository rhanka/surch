# OpenSearch Compatibility Tests

This directory holds replay fixtures and integration tests derived from the pinned OpenSearch reference in `docs/portage/REFERENCES.md`.

Every compatibility test must compare Surch behavior with an upstream OpenSearch oracle or with a recorded oracle fixture.
