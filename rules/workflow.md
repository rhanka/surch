# Legacy Workflow Notes

Canonical active workflow guidance is now `AGENTS.md`.

Use this file only as historical context for the original portage phase.
For current work:

- global tracking lives in `PLAN.md`
- branch/lane tracking lives in `plan/*.md`
- user-facing status uses the fixed-width `Fait / A faire / Attendus`
  format defined in `AGENTS.md`
- Superpowers may guide local execution, but persistent reporting and
  pilotage normalize back into `AGENTS.md`, `PLAN.md`, and `plan/*.md`

Do not use the old `develop` / release-only branch model from the
initial portage phase unless the conductor explicitly reinstates it.
